#include <iostream>
using namespace std;
int main(){
    /* whenever you insert a new element in a 
     vector array then the array doubles it size
     it creates a new vector of double size and all elements 
     are copied , and this functionality is included in 
     our stl library

     Although vector::push_back takes O(1) time , but in some cases it may take
     O(n) time
*/
    return 0;
}