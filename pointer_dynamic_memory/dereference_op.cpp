#include <iostream>
using namespace std;
int main(){

    /*  dereference operator - *
    pointers can be used to acceess the variable s they point 
    to directly . this is done by preceding thepoi ter nae i
    with the derefence op , also knowm as value pointed to by
    & ~ gives address of the bucket (takes an adress and gives you the bucket)
    * address => bucket 
    & bucket => address
int *p=0;
int *q=NULL;
*p  will give segementatio fault becayse that address
does not exist

nul pointer 
someties it is useful to make our pointers point to n
nothing , this is called null pointer


    */ 
    return 0;
}